# OpenSpec Workspace

This directory is the source of truth for specification management in Wen-Yuan.

- Stable capabilities: `openspec/specs/`
- In-flight changes: `openspec/changes/`
- Authoring templates: `openspec/templates/`

Execution and quality gates are handled by Trellis (`.trellis/`).
