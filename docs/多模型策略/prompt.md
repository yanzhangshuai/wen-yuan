你现在的角色不是直接写代码的工程师，而是一个高级技术规划师 / 系统架构师 / 重构项目经理 / Codex 任务拆解设计师。

你的任务不是继续分析模型优劣，而是基于我提供的“书籍解析模型选型报告”，输出一份完整、严格、可执行、适合交给 Codex 执行代码修改的工程实施文档。

你的职责边界
你这次不要直接输出代码实现，也不要直接写补丁。
你的职责是：

完整解析我提供的报告
提取报告中列出的所有问题、建议、风险、待验证项
将这些内容整理成系统性的整改计划
设计一套混合模型按阶段可配置的完整方案
输出一份详细到可以交给 Codex 逐项执行的任务文档
你的输出必须满足两个用途：

给人看：项目负责人和开发者可以据此理解改造范围、排期、依赖和风险
给 Codex 执行：Codex 可以据此逐项修改代码，尽量减少歧义
特别约束
本次改造有以下明确前提，请你严格遵守：

不需要兼容老代码
不需要兼容老数据
允许大范围重构
如果旧设计与新方案冲突，可以直接废弃、替换、删除
不要输出任何“为了兼容旧逻辑而保留分支”的设计
不要要求写旧数据迁移兼容代码
优先采用结构更清晰、长期更易维护的新架构
任务文档必须适合交给 Codex 执行，因此必须足够严格、具体、可验收
输入材料
以下是上一轮分析报告，请你完整吸收，并以此为基础输出整改与实施方案：

多模型策略.md 文件便是上一轮报告完整内容,清认真详细分析此文件

本次任务目标
请生成一份完整的工程实施文档，文档名称为：

《书籍解析系统问题整改与混合模型配置重构实施方案》
这份文档必须覆盖以下 4 大目标：

目标 1：处理报告列出的所有问题
你必须逐条处理上一份报告中提到的所有问题，不能遗漏，至少包括但不限于：

单模型一刀切问题
无 fallback 机制问题
Prompt 过于庞大问题
system / user message 未分离问题
Prompt 中泛化称谓示例硬编码问题
分片过小问题
mergeChunkResults 仅 flatMap、无去重/冲突处理问题
Phase 1 长章节无长度保护问题
章节串行、无 pipeline 重叠问题
无 token 计数、无成本可观测性问题
JSON 解析容错有限问题
DeepSeekClient 未设置 max_tokens 问题
Provider Client 缺少 temperature / maxTokens 参数化问题
重复的模型配置读取逻辑问题
按阶段配置模型缺失问题
章节并发过低问题
Phase 1 分片保护问题
A/B 测试缺失问题
长期指标观测缺失问题
报告中列出的所有高优先级 / 中优先级 / 低优先级问题
报告中列出的所有高置信结论 / 待验证结论
输出要求
请先输出一份问题整改总表，每个问题必须包含：

问题 ID
问题名称
问题类别
所属模块
涉及文件
严重程度（P0 / P1 / P2 / P3）
问题描述
根因分析
推荐修复方案
是否需要改数据库
是否需要改后端
是否需要改前端
是否需要测试
是否存在任务依赖
验收标准
注意：不能遗漏报告中的任何问题。

目标 2：设计混合模型策略方案
你必须把报告中推荐的混合模型策略，真正落地成一套完整的产品与工程方案，而不是停留在建议层面。

核心业务目标
支持“按阶段配置模型”，并允许用户在前端自定义任意阶段使用哪个模型。

必须满足的要求
前端允许用户为每个阶段单独选择模型
只能选择已启用（isEnabled = true）的模型
需要为前端每个阶段模型选择框设置推荐模型.
Phase 1 名册发现：通义千问 Max
Phase 2 分片提取：DeepSeek V3
章节验证：通义千问 Plus
Phase 5 称号溯源：通义千问 Max
全书验证：通义千问 Max
fallback：通义千问 Plus
要支持 Book 级配置
要支持 AnalysisJob 级配置覆盖
要考虑未来新增阶段时的可扩展性
所有模型选择都必须由后端再次校验，不能信任前端
所有模型选项都必须来源于“当前已启用模型”
需要明确“全局默认配置 / Book 配置 / AnalysisJob 配置 / 运行时覆盖”四层优先级
本次不需要兼容旧设计，可以直接设计更清晰的新结构
你必须设计的内容
请完整输出以下方案：

A. 阶段模型配置方案
需要明确：

系统中支持哪些阶段
每个阶段的职责
每个阶段如何绑定模型
fallback 如何设计
是否支持阶段级参数覆盖（temperature / maxTokens / topP / responseFormat）
B. 数据库设计方案
请给出完整数据库重构设计，包括：

推荐的新表结构
是否新增“模型策略配置表”
字段设计
外键关系
索引建议
约束规则
migration 方案
回滚方案
必须考虑：

全局默认模型策略
Book 级模型策略
AnalysisJob 级模型策略
只能引用 ai_models 表中的模型
只能引用已启用模型
模型被禁用/删除时的行为
注意：本次不要考虑老数据兼容，也不要设计旧结构兼容逻辑。

C. 后端设计方案
请详细设计后端重构方案，包括：

运行时模型解析器如何设计
阶段模型选择逻辑如何实现
fallback 路由如何实现
createRuntimeAiClient 如何重构
resolveAnalysisModelConfig / resolveValidationModelConfig 是否废弃、合并、重写
generateJson 接口签名如何升级
Prompt 的 system/user message 结构化如何设计
Provider 能力抽象如何统一
temperature / maxTokens / responseFormat / usage 等参数如何透传
只能选择启用模型的后端校验逻辑放在哪层
DTO / Zod / validator 如何设计
错误处理、日志、成本统计、usage 观测如何设计
D. 前端设计方案
请详细设计前端配置方案，包括：

配置入口放在哪里
Book 配置页和任务启动页如何展示阶段模型设置
每个阶段的模型选择 UI 怎么设计
只展示已启用模型的逻辑
如何恢复推荐配置
如何提示“当前推荐模型不存在/未启用”
提交 payload 如何定义
前端表单结构如何设计
校验规则和错误提示如何设计
E. 配置优先级与解析规则
请明确规定：

全局默认配置
Book 级配置
AnalysisJob 级配置
运行时临时覆盖
这四层如何合并，冲突时谁覆盖谁。
请给出：

优先级规则说明
合并策略
冲突处理策略
降级逻辑
伪代码
目标 3：输出适合 Codex 执行的详细任务文档
这是最重要的部分。

因为我后续要把你的产出交给 Codex 执行代码修改，所以你必须把实施方案写成一份非常严格、尽量减少歧义、适合自动代理执行的任务文档。

必须包含以下部分

实施总览
本次改造目标
改造范围
明确不在本次改造范围的内容
预期收益
风险点
分阶段实施计划
请把改造拆分成多个阶段，例如：
Phase A：架构重构准备与问题收敛
Phase B：数据库与模型策略配置重构
Phase C：后端模型解析与运行时路由重构
Phase D：Provider 抽象与 Prompt 结构重构
Phase E：前端阶段模型配置能力
Phase F：fallback、可观测性与成本统计
Phase G：分片、并发、长章节保护优化
Phase H：测试、验收与发布准备
每个阶段都必须写：

阶段目标
具体改动项
涉及文件或模块
前置依赖
风险
验收标准
3. Codex 可执行任务清单
请把所有改造继续细化成任务清单。每个任务必须包含：

唯一任务编号（如 TASK-001）
任务标题
任务目标
修改类型（新增 / 修改 / 删除 / 重构）
涉及文件
详细执行说明
注意事项
前置依赖
完成标准
要求：

尽量细化到文件和职责
如果一个任务过大，请拆成多个子任务
不要使用“适当处理”“视情况调整”这种模糊表述
要尽量让 Codex 可以按顺序执行
4. 文件级修改清单
请按文件维度列出：

需要修改的现有文件
需要新增的文件
建议删除、废弃、替换的旧文件/旧方法
每个文件修改的目的
文件之间的依赖顺序
5. API / DTO / Schema 设计
请输出详细定义：

API 路由建议
Request / Response 结构
DTO 字段定义
Zod / validator 规则
Prisma schema 草案
枚举定义
前后端字段名约定
6. 测试计划
请给出完整测试计划，包括：

单元测试
集成测试
回归测试
手工验收测试
fallback 测试
模型禁用/删除场景测试
并发测试
长章节测试
性能测试
A/B 测试建议
可观测性指标验证
7. 发布与回滚方案
请说明：

migration 发布顺序
是否需要 feature flag
是否建议灰度发布
如果上线失败如何回滚
如何避免影响正在运行的任务
目标 4：输出风格必须适合作为正式工程文档
请将整份内容写成工程实施文档风格，而不是聊天式回答。

要求：

结构化
严格
具体
可执行
面向项目实施
面向 Codex 执行
输出格式要求
请严格按以下 Markdown 结构输出：

书籍解析系统问题整改与混合模型配置重构实施方案

文档问题整改总表
总体目标与改造范围
混合模型策略设计
3.1 阶段划分与职责
3.2 默认推荐模型方案
3.3 配置优先级规则
3.4 降级与 fallback 规则
数据库重构方案
4.1 数据模型设计
4.2 表结构与字段说明
4.3 索引与约束
4.4 Migration 方案
4.5 回滚方案
后端重构方案
5.1 运行时模型解析器
5.2 模型路由与 fallback 设计
5.3 Provider 抽象重构
5.4 Prompt 结构重构
5.5 参数透传设计
5.6 可观测性与成本统计
5.7 错误处理与日志方案
前端重构方案
6.1 页面与入口设计
6.2 表单与交互设计
6.4 只允许选择启用模型的规则
6.5 错误提示与空状态设计
分阶段实施计划
Codex 可执行任务清单
文件级修改清单
API / DTO / Schema 详细定义
测试计划
发布计划与回滚方案
推荐实施顺序
强约束
你这次的任务是生成工程实施文档和任务文档，不是直接写代码
必须覆盖报告中的所有问题
必须输出前端、后端、数据库、测试、发布的完整方案
必须设计“混合模型按阶段可配置”的整套方案
所有模型选择都必须保证“只能选择已启用模型”
不需要兼容老代码或老数据
允许大范围重构
旧逻辑与新设计冲突时，可以明确提出删除、替换、废弃
不要输出任何旧逻辑兼容方案
文档必须足够严格，适合直接交给 Codex 执行
不要写空泛建议，必须写清楚“改什么、改哪里、为什么、如何验收”
最后要求
请以“正式工程实施文档”风格输出。
不要写成聊天回答，不要省略任务细节，不要只给原则性建议。最后的输出必须写入本地文件，命名为 `书籍解析系统问题整改与混合模型配置重构实施方案.md`，并且内容必须覆盖上述所有要求和结构。
请开始。
