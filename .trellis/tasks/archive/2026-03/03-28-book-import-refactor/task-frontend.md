# 前端执行任务：书籍导入流程重构

> 关联任务：`03-28-book-import-refactor`  
> 更新时间：2026-03-28

## 1. 变更清单

- [ ] `src/app/admin/books/import/page.tsx`：步骤2标签由"章节预览"改为"预览&确认"。
- [ ] `src/lib/services/books.ts`：确认 `createBook`/`fetchChapterPreview`/`confirmBookChapters` 接口调用无需变更（契约不变）。

## 2. 说明

前端 API 调用契约不变，只有步骤标签文案调整：

```tsx
// 修改前
{s === 2 ? "章节预览" : ...}

// 修改后
{s === 2 ? "预览&确认" : ...}
```

前端流程验证：
1. Step 1 上传后，立即可进入 Step 2（章节预览）。
2. Step 2 点击"生成并检查章节"，应正确显示切分后章节（含全角空格标题）。
3. Step 3 点击"启动解析任务"，确认章节写入后触发解析。

## 3. 验证点

1. 四步进度条正常显示，标签文字正确。
2. 上传 + 确认 + 解析全流程在开发环境下跑通。
